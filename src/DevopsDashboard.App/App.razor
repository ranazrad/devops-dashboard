<Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        <!-- explicit layout namespace path -->
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(DevopsDashboard.App.Layout.MainLayout)" />
        <FocusOnNavigate RouteData="@routeData" Selector="h1" />
    </Found>
    <NotFound>
        <PageTitle>Not found</PageTitle>
        <LayoutView Layout="@typeof(DevopsDashboard.App.Layout.MainLayout)">
            <p role="alert">Sorry, there's nothing at this address.</p>
        </LayoutView>
    </NotFound>
</Router>
